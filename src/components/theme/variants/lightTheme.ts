import { Theme } from '../theme.types';

export const LIGHT_THEME: Theme = {
  cText: '#39393c',
  cBackground: '#e5d3d8',
  cAccent: '#4141a2',
  cAccentBackground: '#afcce9',
  cStroke: '#39393c',
};

export const lightTheme: Theme = {
  ...LIGHT_THEME,
};
